[package]
name = "specgate-harness"
version = "0.1.0"
edition = "2024"

[features]
test-util = []

[dependencies]
serde_json.workspace = true
serde_yaml.workspace = true
specgate-types = { path = "../specgate-types" }
time.workspace = true

[dev-dependencies]
specgate-rust-backend = { path = "../specgate-rust-backend" }
