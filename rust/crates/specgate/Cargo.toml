[package]
name = "specgate"
version.workspace = true
edition.workspace = true
description = "Native CTSC capture annotations for deterministic differential testing"
license.workspace = true
repository.workspace = true
homepage.workspace = true
readme = "README.md"
keywords = ["specgate", "testing", "verification", "ctsc", "tracing"]
categories = ["development-tools::testing"]
rust-version.workspace = true

[features]
default = []

[dependencies]
specgate-annotations-macros = { workspace = true }
specgate-runtime = { workspace = true }

[lints]
workspace = true
