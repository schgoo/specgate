[package]
name = "specgate-rust-backend"
version = "0.1.0"
edition = "2024"

[dependencies]
serde.workspace = true
serde_json.workspace = true
serde_yaml.workspace = true
specgate-harness = { path = "../specgate-harness" }
specgate-types = { path = "../specgate-types" }
