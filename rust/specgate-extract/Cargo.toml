[package]
name = "specgate-extract"
version = "0.1.0"
edition = "2024"

[dependencies]
specgate-core = { path = "../specgate-core" }
clap = { version = "4", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
