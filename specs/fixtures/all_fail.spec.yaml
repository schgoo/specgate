# yaml-language-server: $schema=../../spec-schema.json
# Fixture: all cases fail.
name: all_fail
binding: mock
target: test
outcome: Complete
outputs:
  when Complete:
    report: RunReport
cases:
  - name: first_case
    desc: First case fails.
    inputs:
      mock_result: fail
    expected:
      outcome: Complete
  - name: second_case
    desc: Second case fails.
    inputs:
      mock_result: fail
    expected:
      outcome: Complete
