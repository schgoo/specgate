# yaml-language-server: $schema=../../spec-schema.json
name: simple_pass
binding: mock
target: test
outcome: Complete
outputs:
  when Complete:
    report: RunReport
cases:
  - name: basic_case
    desc: The basic case passes.
    expected:
      outcome: Complete
