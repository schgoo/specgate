# yaml-language-server: $schema=../../spec-schema.json
name: build_error
binding: mock
target: test
outcome: Complete
outputs:
  when Complete:
    report: RunReport
cases:
  - name: build_case
    desc: The mock backend fails during build.
    expected:
      outcome: Complete
