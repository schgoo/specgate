# yaml-language-server: $schema=../../spec-schema.json
name: bad_binding
binding: nonexistent
target: test
outcome: Complete
outputs:
  when Complete:
    report: RunReport
cases:
  - name: binding_case
    desc: The binding cannot be resolved.
    expected:
      outcome: Complete
