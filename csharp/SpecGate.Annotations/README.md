# SpecGate.Annotations

Compile-time metadata attributes for SpecGate CTSC discovery of C# operations,
setups, semantic inputs, structured types, and declared exceptions.

This package does not instrument or record runtime traces. The `specgate`
command reflects over the compiled target assembly.
