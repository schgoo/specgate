{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SpecGate Binding File",
  "description": "Schema for SpecGate binding files — connects a spec to a language-specific implementation project.",
  "type": "object",
  "required": ["language", "project_root"],
  "additionalProperties": false,
  "properties": {
    "language": {
      "type": "string",
      "description": "Target language identifier. Selects the codegen backend and compile-time registry reader.",
      "enum": ["rust", "csharp"]
    },
    "project_root": {
      "type": "string",
      "description": "Path to the implementation project, relative to the binding file. The harness builds this project to populate the compile-time annotation registry, then generates tests here."
    }
  }
}
