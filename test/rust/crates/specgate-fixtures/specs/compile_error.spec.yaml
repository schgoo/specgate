spec_version: "0.3.0"
name: fixture.compile_error
binding: binding.yaml

operations:
  broken:
    outcome:
      oneof: [Complete]
    outputs:
      when Complete:
        result: i32

cases:
  - name: broken_fn
    desc: Source has a syntax error
    operation: broken
    expected:
      - broken.result: "42"
