spec_version: "0.4.0"
name: fixture.compile_error
binding: binding.yaml

operations:
  broken:
    outputs: [{ $result: i32 }]

cases:
  - name: broken_fn
    desc: Source has a syntax error
    operation: broken
    expected:
      - $result: "42"
