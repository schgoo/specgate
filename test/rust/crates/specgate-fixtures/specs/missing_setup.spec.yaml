name: fixture.missing_setup
binding: binding.yaml
cases:
  - name: increment_once
    desc: References a setup that doesn't exist
    setup: make_counter
    operation: increment
    expected:
      - count: "1"