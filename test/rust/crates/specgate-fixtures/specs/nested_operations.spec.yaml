name: fixture.nested_operations
binding: binding.yaml
cases:
  - name: transfer_50
    desc: Transfer calls withdraw then deposit, balance restored
    setup: make_account
    operation: transfer
    inputs: { amount: 50 }
    expected:
      deposit.balance: "100"