spec_version: "0.4.0"
name: fixture.simple_output_setup
binding: binding.yaml

operations:
  double:
    outputs:
    - $result: i32

cases:
  - name: setup_fills_primitive_param
    desc: A setup returning a primitive fills the operation parameter by type
    operation: double
    expected:
      - $result: "42"
