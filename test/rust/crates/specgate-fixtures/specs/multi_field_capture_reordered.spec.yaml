name: fixture.multi_field_capture_reordered
binding: binding.yaml
cases:
  - name: withdraw_50
    desc: Just assert final values
    setup: make_account
    operation: withdraw
    inputs: { amount: 50 }
    expected:
      - balance: "50"
      - transaction_count: "1"