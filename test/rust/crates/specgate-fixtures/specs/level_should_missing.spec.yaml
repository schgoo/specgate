spec_version: "0.4.0"
name: fixture.level_should_missing
binding: binding.yaml

operations:
  recommended_feature:
    outputs: [{ $result: i32 }]

cases:
  - name: recommended_not_implemented
    level: should
    desc: SHOULD operation not annotated — should warn, not error
    operation: recommended_feature
    inputs: {}
    expected:
      - $result: "done"
