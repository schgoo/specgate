name: fixture.missing_operation
binding: binding.yaml
cases:
  - name: increment_once
    desc: References an operation not annotated
    setup: make_counter
    operation: increment
    expected:
      count: "1"