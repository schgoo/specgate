name: fixture.readonly_operation
binding: binding.yaml
cases:
  - name: read_count
    desc: Read-only returns value without changing state
    setup: make_counter
    operation: get_count
    expected:
      - get_count.result: "42"