spec_version: 0.4.0
name: fixture.readonly_operation
binding:
  - binding.yaml
  - csharp.yaml
operations:
  get_count:
    outputs:
    - $result: i32
cases:
- name: read_count
  desc: Read-only operation returns value without state change
  operation: get_count
  expected:
  - count: 42
  - $result: 42
