name: fixture.setup_with_params
binding: binding.yaml
cases:
  - name: start_at_10
    desc: Counter starts at 10
    setup: make_counter
    operation: increment
    inputs: { initial: 10 }
    expected:
      - make_counter.initial: "10"
      - count: "10"
      - run: increment
      - count: "11"