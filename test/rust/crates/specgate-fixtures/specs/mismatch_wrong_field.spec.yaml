spec_version: "0.4.0"
name: fixture.mismatch_wrong_field
binding: binding.yaml

operations:
  increment:
    outputs: [count]

cases:
  - name: increment_once
    desc: Expects 'total' but source produces 'count'
    operation: increment
    expected:
      - total: "1"
