name: fixture.mismatch_wrong_field
binding: binding.yaml
cases:
  - name: increment_once
    desc: Expects 'total' but source produces 'count'
    setup: make_counter
    operation: increment
    expected:
      - total: "1"