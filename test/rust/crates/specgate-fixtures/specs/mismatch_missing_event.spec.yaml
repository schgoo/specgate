spec_version: "0.4.0"
name: fixture.mismatch_missing_event
binding: binding.yaml

operations:
  add:
    inputs: { a: i32, b: i32 }
    outputs: [add.result]

cases:
  - name: add_2_3
    desc: Expects a field that doesn't exist
    operation: add
    inputs: { a: 2, b: 3 }
    expected:
      - add.result: "5"
      - add.extra: "bonus"
