name: fixture.mismatch_second_step
binding: binding.yaml
cases:
  - name: increment_then_decrement
    desc: Second step wrong expected value
    setup: make_counter
    steps:
      - operation: increment
      - operation: decrement
    expected:
      decrement.count: "999"