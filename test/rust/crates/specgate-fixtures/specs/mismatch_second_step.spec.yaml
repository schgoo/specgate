name: fixture.mismatch_second_step
binding: binding.yaml
cases:
  - name: increment_then_decrement
    desc: Wrong value after second step
    setup: make_counter
    steps:
      - operation: increment
      - operation: decrement
    expected:
      - run: decrement
      - count: "999"