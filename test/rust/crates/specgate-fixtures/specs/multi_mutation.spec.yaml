name: fixture.multi_mutation
binding: binding.yaml
cases:
  - name: double_increment
    desc: Two mutations captured individually
    setup: make_counter
    operation: increment_twice
    expected:
      increment_twice.count: "2"