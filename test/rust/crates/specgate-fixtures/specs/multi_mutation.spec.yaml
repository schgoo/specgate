name: fixture.multi_mutation
binding: binding.yaml
cases:
  - name: double_increment
    desc: Two mutations both captured in order
    setup: make_counter
    operation: increment_twice
    expected:
      - count: "0"
      - run: increment_twice
      - count: "1"
      - count: "2"