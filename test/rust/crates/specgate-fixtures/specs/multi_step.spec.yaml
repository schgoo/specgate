name: fixture.multi_step
binding: binding.yaml
cases:
  - name: increment_then_decrement
    desc: Increment then decrement — count back to 0
    setup: make_counter
    steps:
      - operation: increment
      - operation: decrement
    expected:
      decrement.count: "0"