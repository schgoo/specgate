name: fixture.statemachine_counter_wrong
binding: binding.yaml
cases:
  - name: increment_once
    desc: Wrong expected — 999 instead of 1
    setup: make_counter
    operation: increment
    expected:
      - count: "999"