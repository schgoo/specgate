name: fixture.checkpoint_inline
binding: binding.yaml
cases:
  - name: process_hello
    desc: Processing hello uppercases and trims
    operation: process
    inputs: { data: " hello " }
    expected:
      after_upper: " HELLO "
      process.result: "HELLO"