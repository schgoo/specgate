name: fixture.checkpoint_inline
binding: binding.yaml
cases:
  - name: process_hello
    desc: Checkpoint captures intermediate value
    operation: process
    inputs: { data: " hello " }
    expected:
      - run: process
      - after_upper: " HELLO "
      - process.result: "HELLO"