spec_version: "0.4.0"
name: test.source_check_pass
binding: binding.yaml
operations:
  compute:
    inputs: { req: ComputeRequest }
    outputs: [result]
  noop:
    outputs: [result]
types:
  ComputeRequest:
    x: i32
    y: i32
cases:
  - name: noop_case
    desc: Trivial runnable case so the spec is harnessable
    operation: noop
    expected:
      - result: "0"
