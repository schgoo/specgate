spec_version: "0.4.0"
name: test.bad_target
binding: binding.yaml
target: ghost
operations:
  noop:
    outputs: [result]
cases:
  - name: noop_case
    desc: Trivial runnable case
    operation: noop
    expected:
      - result: "0"
