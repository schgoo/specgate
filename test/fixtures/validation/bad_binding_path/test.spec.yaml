spec_version: "0.4.0"
name: test.bad_binding_path
binding: does_not_exist.yaml
operations:
  noop:
    outputs: [result]
cases:
  - name: noop_case
    desc: Trivial runnable case
    operation: noop
    expected:
      - result: "0"
